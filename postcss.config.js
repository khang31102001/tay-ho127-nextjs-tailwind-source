module.exports = {
  // Tailwind xử lý utility class trước.
  plugins: {
    // Plugin TailwindCSS biên dịch class thành CSS thật.
    tailwindcss: {},
    // Autoprefixer tự thêm prefix trình duyệt khi cần.
    autoprefixer: {},
  },
};
